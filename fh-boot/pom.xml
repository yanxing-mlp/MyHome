<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.familyhome</groupId>
    <artifactId>family-home-server</artifactId>
    <version>1.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>fh-boot</artifactId>
  <name>fh-boot</name>
  <description>
    唯一启动模块：main、全局配置、静态资源映射、拦截器链（一期为空）、全局异常处理、Flyway。
    不含任何业务逻辑；一期也不依赖 album-api / recipe-api（C 端接口全部推迟到二期）。
  </description>

  <dependencies>
    <!-- 聚合四个业务域的 biz，由 Spring 扫描装配 -->
    <dependency>
      <groupId>com.familyhome</groupId>
      <artifactId>fh-module-file-biz</artifactId>
    </dependency>
    <dependency>
      <groupId>com.familyhome</groupId>
      <artifactId>fh-module-album-biz</artifactId>
    </dependency>
    <dependency>
      <groupId>com.familyhome</groupId>
      <artifactId>fh-module-recipe-biz</artifactId>
    </dependency>
    <dependency>
      <groupId>com.familyhome</groupId>
      <artifactId>fh-module-vault-biz</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jdbc</artifactId>
    </dependency>

    <!-- 分页插件需要 JSqlParser（MyBatis-Plus 3.5.9+ 拆包） -->
    <dependency>
      <groupId>com.baomidou</groupId>
      <artifactId>mybatis-plus-jsqlparser</artifactId>
    </dependency>

    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- Flyway：MySQL 支持自 Flyway 10 起拆到独立 artifact -->
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-mysql</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>family-home-server</finalName>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <mainClass>com.familyhome.boot.FamilyHomeApplication</mainClass>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>repackage</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
