-- 概念改名：「账号本」→「密码本」（2026-09-21 用户要求全局改名，菜单/代码/注释一起改）。
-- 只动表注释：这一域的模型、列、两态 deleted 都没变，换掉的是给人看的那个词。
-- V400 已经应用过、按 Flyway 规则不可回改（改了校验和直接起不来），所以注释改在这里
-- —— 与 V309/V310 那两条纯注释迁移同一写法。

ALTER TABLE `vault_account` COMMENT = '密码本（两态删除，密码加密存储）';
