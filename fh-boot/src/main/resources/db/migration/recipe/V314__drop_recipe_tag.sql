-- 删除"菜品标签"这个概念：标签字典 + 菜谱-标签关联两张表一起下线。
--
-- 标签当初是"一菜多标签"的自由分类（午餐/晚餐/荤/素这类），但它的用途都已被别的东西取代：
--   * "这道菜属于哪一组"由 recipe_category（一菜一分组，C 端点餐页左栏就是它）承担；
--   * "这道菜怎么做"由 recipe_practice_group / option（辣度、糖等，随购物车落库）承担；
--   * 剩下那点用途（按标签筛菜、卡片上挂几个 chip）在 V313 删掉 C 端"餐段"标签条之后就没人消费了。
-- 于是它退化成一个纯装饰字段：B 端菜谱表单里一个可填可不填的多选，C 端展示几个谁也不点的灰色 chip。
-- 与其留一个没有语义的字典让人继续纠结"该不该给它加标签"，不如整体删掉。
--
-- 现在两张表都是空的（标签数据在 V313 里随餐段一起删了，之后 B 端没再新建），这一步不丢用户数据。

DROP TABLE IF EXISTS `recipe_tag_rel`;

DROP TABLE IF EXISTS `recipe_tag`;
