<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.familyhome</groupId>
    <artifactId>fh-module-recipe</artifactId>
    <version>1.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>fh-module-recipe-biz</artifactId>
  <name>fh-module-recipe-biz</name>
  <description>菜谱域实现：菜品 CRUD + 多图、标签/类型字典（级联解绑）、多对多 EXISTS 过滤分页。</description>

  <dependencies>
    <dependency>
      <groupId>com.familyhome</groupId>
      <artifactId>fh-module-recipe-api</artifactId>
    </dependency>
    <!-- 跨域依赖：只允许依赖对方 -api，禁止依赖 file-biz -->
    <dependency>
      <groupId>com.familyhome</groupId>
      <artifactId>fh-module-file-api</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>com.baomidou</groupId>
      <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
    </dependency>
  </dependencies>
</project>
